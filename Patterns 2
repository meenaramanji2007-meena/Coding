#include <stdio.h>

int main()
{
    int n;
    scanf("%d",&n);
    for(int i=1;i<=n;i++)
    {
        char current_char = 'A' + n - i;
        for(int j=1;j<=i;j++)
        {
            printf("%c ",current_char);
        }
        printf("\n");
    }
    
}
